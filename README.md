# pylance-repro

This repo contains repro workspaces and supporting files for Pylance issues.

The `pylance-issues/` folder contains one workspace per issue. A `-draft` suffix means the scenario is still being confirmed and should not be treated as final yet.
