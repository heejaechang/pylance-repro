# Issue 7554 Draft

Use this folder to investigate the `Unpack[...]` kwargs tokenization scenario in `scenarios/issue_7554.py`.

This workspace is still marked as draft because the scenario needs stronger confirmation before it should be treated as final.

## Current Repro Focus

Open `scenarios/issue_7554.py` and compare the call-site keyword tokenization for the typed `Unpack[...]` kwargs call against the untyped control call.

## Draft Status

- Keep this folder as `7554-draft`.
- Use it for investigation, but do not treat it as final yet.
